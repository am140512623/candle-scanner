name: Bot - Mid-Cap Stocks

# US stocks $2B - $10B. Fires right after a candle CLOSES:
#   weekly  -> Saturday (the Mon-Fri weekly candle is finalized)
#   monthly -> 1st of the month (the previous month's candle is finalized)
on:
  schedule:
    - cron: "10 6 * * 6"   # weekly  (Saturday 06:10 UTC)
    - cron: "10 6 1 * *"   # monthly (1st of month 06:10 UTC)
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python scan_segment.py
        env:
          SEGMENT: stock_mid
          SCAN_MODE: ${{ github.event.schedule == '10 6 1 * *' && 'monthly' || 'weekly' }}
          SEG_TOKEN: ${{ secrets.STOCK_MID_BOT_TOKEN }}
