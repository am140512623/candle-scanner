charts/
.monthly_state.txt
__pycache__/
*.pyc
WhatsApp*.jpeg
# Telegram bot tokens / secrets / GitHub PAT -- never commit these
bot sec.txt
pat.txt
*sec*.txt
*.token
api.txt
apitxt
*api*.txt
*token*.txt
*.key

# Result/state data -- these now live ONLY in the private data repo
# (candle-data). Workflows pull them at runtime; never commit them here.
signals.csv
results.csv
summary.csv
.index_signals_seen.txt

# Local backtest / research outputs -- regenerated on demand, not source.
*.log
*_trades.csv
*_equity.png
